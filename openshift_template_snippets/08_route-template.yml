- apiVersion: v1
  kind: Route
  metadata:
    annotations:
    labels:
      app: ${APPLICATION_NAME}
    name: "{{ DOMAIN }}"
  spec:
    host: "{{ DOMAIN }}"
    tls:
      termination: edge
    to:
      kind: Service
      name: webproxy
      weight: 100
    wildcardPolicy: None
